<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns="http://www.springframework.org/schema/security"
             xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <beans:import resource="global-application-context.xml"/>
    <global-method-security pre-post-annotations="enabled"/>
    <http pattern="/resources/**" security="none"/>
    <http pattern="/login" security="none"/>
    <http pattern="/register" security="none"/>
    <http pattern="/activation/**" security="none"/>


    <http>
        <!-- Be careful, the order matters -->
        <intercept-url pattern="/" access="permitAll"/>
        <intercept-url pattern="/logout.jsp" access="permitAll"/>
        <intercept-url pattern="/admin/**" access="hasRole('Administrator')"/>
        <intercept-url pattern="/buyer/**" access="hasRole('Seller')"/>
        <intercept-url pattern="/seller/**" access="hasRole('Buyer')"/>
        <intercept-url pattern="/**" access="isAuthenticated()"/>

        <!-- Use default provided form. See docs to customize. -->
        <form-login login-page="/login"
                    default-target-url="/"
                    authentication-failure-url="/error"
                    username-parameter="username"
                    password-parameter="password"/>
        <logout logout-success-url="/logout"/>
        <!-- enable csrf protection -->
        <csrf/>

        <access-denied-handler error-page="/denied"/>
        <!-- Custom page for access denied pages -->
        <logout logout-url="/" delete-cookies="JSESSIONID"/>

        <!-- <remember-me /> requires user details services-->

    </http>

    <beans:bean id="userAuthenticationProvider"
                class="ca.ulaval.glo4003.housematch.spring.web.security.UserAuthenticationProvider">
        <beans:constructor-arg ref="userService"/>
    </beans:bean>


    <authentication-manager>
        <authentication-provider ref="userAuthenticationProvider"/>
    </authentication-manager>

</beans:beans>